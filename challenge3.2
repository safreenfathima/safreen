class student:

    def__init__(self.name,roll_number,cgpa):
      self.name=name
      self.roll_number=roll_number
      self.cgpa=cgpa

def sort_students(student-list):
  sorted_students=sorted(student_list):
                  key=lambdastudent:student.cgpa,
                  reverse=true)
  reverse sorted_students